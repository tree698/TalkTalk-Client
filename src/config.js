export const retryConfig = {
  retries: 5,
  initialDelayMs: 100,
};

export const paginationForCarousel = {
  limit: 4,
  offset: 0,
};

export const paginationForAllDrawings = {
  limit: 8,
  offset: 0,
};

export const paginationForMyDrawingsAndSearchedDrawings = {
  limit: 100,
  offset: 0,
};
